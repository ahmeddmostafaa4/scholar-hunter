"""Scholar Hunter — local Flask server (stub; built in Phase 6).

Run from the VS Code integrated terminal:
    python app.py
then open http://localhost:5000 in a browser. Local dev only — no deployment.
"""

# Phase 6 fills this in.

if __name__ == "__main__":
    print("Scholar Hunter app stub — Flask server arrives in Phase 6.")
